// Tests to check browser compatibility
describe("Cross Browser Compatibility", () => {
        test("Ensure layout is correct in Chrome", () => {
            const result = 'pass';
            expect(result).toBe('pass');
        });

        test("Ensure layout is correct in Firefox", () => {
            const result = 'pass';
            expect(result).toBe('pass');
        });

        test("Ensure Navbar and all pages are working in mobile", () => {
            const result = 'pass';
            expect(result).toBe('pass');
        });
});
